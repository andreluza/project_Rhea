# going through the folders
require (here) 

# standardization
require(vegan) 

# models
library(R2WinBUGS) 
require(jagsUI)

# space
require(rgdal)
require(raster)
require(rgeos)

# plots, mapps, arrays
require(ggplot2)
require(gridExtra)



