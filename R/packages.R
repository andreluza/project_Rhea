# going through the folders
require (here) 

# standardization
require(vegan) 

# models
library(R2WinBUGS) 
require(jagsUI)
require(brms)
require(emmeans)

# space
require(rgdal)
require(raster)
require(rgeos)
require(rgdal)
require(raster)
require(here)
require(rgeos)
require(sp)
library(spdep)
library(maps)
library(maptools)

# data
library(rgbif)
require(CoordinateCleaner)
library(rvertnet)
require (spocc)


# plots, mapps, arrays
require(ggplot2)
require(gridExtra)
require(rasterVis)
require(viridis)
require(corrplot)

# processing
require(parallel)
library(dplyr)
library(tidyr)
require(dplyr)
library(tidyverse)
library(tidybayes)
require (reshape)
